# --- ядро инференса (CPU) ---
# torch/torchvision ставить с CPU-индекса:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
torchvision
ultralytics            # AGPL-3.0 — для продукта ре-экспортить в ONNX (см. дизайн §4)
opencv-python-headless
numpy
scikit-image
pillow
huggingface_hub

# --- сегментация формы ---
# MobileSAM работает из коробки через ultralytics.models.sam (веса качаются сами).
# Официальный пакет ставить не обязательно (если стоит — используется первым):
# pip install git+https://github.com/ChaoningZhang/MobileSAM.git
# Без ultralytics-SAM и пакета — фолбэк GrabCut.

# --- относительная глубина (опц., флаг --depth) ---
# transformers          # Depth Anything V2 Small (Apache-2.0); установлен в .venv

# --- dev ---
pytest
