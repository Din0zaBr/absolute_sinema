"""Платформа учёта дорожных дефектов: подсистемы 2–5 поверх CV-движка.

CV-движок (подсистема 1) остаётся источником правды — эта надстройка потребляет
его стабильный JSON-контракт (report.py) и ничего в нём не меняет:

- db        — SQLite-хранилище отчётов/дефектов/уведомлений (подсистема 2)
              + жизненный цикл учёта (статус new/in_progress/fixed);
- geo       — геопривязка: журнал ям, EXIF-GPS, разбор «lat lon»;
- notify    — правила уведомлений (несоответствие ГОСТ, подозрение на глубокую яму);
- webapp    — веб-интерфейс на stdlib http.server: загрузка фото → анализ →
              отчёт, дашборд (фильтр по статусу), карта (Leaflet), уведомления,
              смена статуса/удаление отчёта, экспорт реестра /export.csv
              (подсистемы 3–5);
- templates — Jinja2-шаблоны страниц и презентационные хелперы.

Импортов подмодулей здесь нет намеренно: webapp лениво тянет пайплайн с
моделями, и «import road_defect.platform» обязан оставаться дешёвым.

Запуск сервера:
    $env:PYTHONPATH="src"
    .\\.venv\\Scripts\\python.exe -m road_defect.platform.webapp
"""

__all__ = ["db", "geo", "notify", "webapp"]
